.dfx/
.basilisk/
node_modules/
dist/
.svelte-kit/
build/
__pycache__/
*.egg-info/
.pytest_cache/
canister_ids.json
.env
frontend/static/local-network.json

# icp-cli local replica cache (do NOT commit). The mainnet canister-ID
# mappings live in .icp/data and MUST be kept, so they are re-included.
/.icp/*
!/.icp/data/
